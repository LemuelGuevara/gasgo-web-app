
import { Box } from "@mui/joy";
import { styled } from "@mui/system";

const FlexBox = styled(Box)({
  display: "inline-flex",
  flexDirection: "row",
  alignItems: "center",
});

export default FlexBox;
